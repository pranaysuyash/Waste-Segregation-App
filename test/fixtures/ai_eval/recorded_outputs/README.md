# Recorded Outputs

Use one JSONL file per mode/provider snapshot, e.g.:
- `recorded.jsonl`
- `live-2026-05-22-openai.jsonl`

Each row maps to one `caseId` and includes category, confidence, provider,
model, latency, estimatedCostUsd, and cacheHit.
